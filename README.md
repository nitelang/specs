This repository contains Nite specification written in markdown.
You can read them right here, or at [this website](https://nitelang.github.io/specs).

Some files in `docs/` directory has `XX-` prefix in it, this is done to explicitly set the right order of reading these pages.